# Python requirements for Kurucz-Korg integration
#
# Install with:
#   pip install -r requirements.txt
#
# Then install kurucz1 emulator:
#   git clone https://github.com/jiadonglee/kurucz1.git
#   cd kurucz1
#   pip install -e .

# Core dependencies
torch>=1.9.0
numpy>=1.20.0

# Optional but recommended
h5py>=3.0.0        # For reading/writing HDF5 training data
matplotlib>=3.3.0   # For plotting
